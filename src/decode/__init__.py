"""Getting from raw recordings to a decoded head-direction angle.

DANDI loading, firing rates, the Isomap embedding and ring fit, and the cache that stores the
result so every metric can read it without repeating the expensive part.
"""
