"""Figure building blocks, shared across experiments.

`base` holds the scaffolding every figure repeats (saving, grid layout, the log-D axis);
`strips` and `curves` hold the two panel grammars this project actually uses.
"""
