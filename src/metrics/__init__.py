"""The quantities measured from a decoded angle, one module per family.

`diffusion` and `timescale` are the diffusion constant and its long-lag behaviour, `bouts` the
per-bout view, `variance` the between/within-animal decomposition. A new metric lands here.
"""
